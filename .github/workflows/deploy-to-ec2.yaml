name: Fullstack-todo server flow

on:
  push:
    branches:
      - deploy

jobs:
  setup-ssh:
    runs-on: ubuntu-latest
    env:
      EC2_SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
      EC2_URL: ${{ secrets.EC2_HOST }}
      EC2_USERNAME: ${{ secrets.EC2_USERNAME }}
    steps:
    - name: get repo
      uses: actions/checkout@v2
      with:
        ref: deploy
    - name: Login to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_EMAIL }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build Docker Image
      run: docker build -t uaral/fullstack-todo-server:latest .

    - name: Push Docker Image
      run: docker push uaral/full-stack-server:latest
    
    - name: Setup SSH for EC2
      uses: omarhosny206/setup-ssh-for-ec2@v1.0.0
      with:
          EC2_SSH_PRIVATE_KEY: $EC2_SSH_PRIVATE_KEY
          EC2_URL: $EC2_URL
    - name: Connect to EC2
      run: |
        ssh -o StrictHostKeyChecking=no $EC2_USERNAME@$EC2_URL "\
        docker pull uaral/fullstack-todo-server:latest &&
        docker stop fullstack-todo-server || true &&
        docker rm fullstack-todo-server }} true &&
        docker run -d --name fullstack-todo-server -p 80:80 uaral/fullstack-todo-server:latest
        "